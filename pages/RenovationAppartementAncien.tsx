import SeoLandingPage from '../components/SeoLandingPage';
import { seoPageConfigs } from '../lib/seoPageConfigs';

export default function RenovationAppartementAncien() {
  return <SeoLandingPage page={seoPageConfigs.renovationAppartementAncien} />;
}
